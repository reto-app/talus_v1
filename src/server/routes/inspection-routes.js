import { withTenantTransaction } from "../../db/transaction.js";import{createInspectionWorkflow}from"../../services/inspection-service.js";import{DomainError}from"../../services/operations-service.js";
export async function inspectionRoutes(app,{pool}){app.post("/api/v1/inspections",async(r,reply)=>{if(!["staff","api"].includes(r.talusContext.actorKind))return reply.forbidden();try{return reply.code(201).send(await withTenantTransaction(pool,r.talusContext,c=>createInspectionWorkflow(c,r.body)))}catch(e){if(e instanceof DomainError)return reply.code(e.status).send({code:e.code});throw e}})}
