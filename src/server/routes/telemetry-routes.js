import{withTenantTransaction}from"../../db/transaction.js";import{ingestTelemetryWorkflow}from"../../services/telemetry-service.js";import{DomainError}from"../../services/operations-service.js";
export async function telemetryRoutes(app,{pool}){app.post("/api/v1/telemetry/ingest",async(r,reply)=>{if(!["device","staff"].includes(r.talusContext.actorKind))return reply.forbidden();try{return reply.code(201).send(await withTenantTransaction(pool,r.talusContext,c=>ingestTelemetryWorkflow(c,r.body)))}catch(e){if(e instanceof DomainError)return reply.code(e.status).send({code:e.code});throw e}})}
